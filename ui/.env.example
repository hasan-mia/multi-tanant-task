# Next.js app
PORT=3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend API (Express, mounted at /api). The browser talks to it directly
# using a Bearer access token returned by /api/auth/login.
NEXT_PUBLIC_API_URL=http://localhost:3001
